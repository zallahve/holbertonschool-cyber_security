# 0x02. Burp Suite Fundamentals

This project covers the basics of Burp Suite, including:
- proxy setup
- browser interception
- DNS hostname resolution overrides
- inspecting the TLS server certificate to retrieve a flag

## Files

- `0-flag.txt` - contains the flag extracted from the server TLS certificate
