# SQL and NoSQL Injections

This project covers basic SQL injection and NoSQL injection concepts in a web application security lab.

## Tasks

### 0. SQLi - Basic Injection Discovery
Identify vulnerable parameters in the application and save the vulnerable parameter name in:

`0-vuln.txt`

For this task, the vulnerable parameter is:

`search`
